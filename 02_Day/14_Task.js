const value1 = 60;
const value2 = 40;
const value3 = 60;
const value4 = 60;

// Using the ternary operator to evaluate a condition
var result = (value1 + value2) > 50 ? (value3 + value4) : (value1 + value2);
console.log(result);
